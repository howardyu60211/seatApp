# npm 12 預設 allow-remote=none，會拒絕安裝非 registry 的 tarball URL。
# xlsx 直接裝 SheetJS 官方 CDN（npm 上的 0.18.5 有 prototype pollution 與
# ReDoS，且 SheetJS 已停止發佈到 npm，永遠不會有修補版），所以必須放行。
# 用 root 而非 all：只允許本專案 package.json 直接宣告的遠端 URL，
# 相依套件自己帶進來的遠端 tarball 仍然被擋。
allow-remote=root
